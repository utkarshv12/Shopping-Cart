# Copy this file to `.env` and fill in the real values. Do NOT commit your real `.env` to version control.

# Option 1: Provide full DSN (uncomment and set)
# MYSQL_DSN=root:changeme@tcp(127.0.0.1:3306)/shopping_cart?charset=utf8mb4&parseTime=True&loc=Local

# Option 2: Provide individual settings (recommended for local dev)
DB_USER=root
DB_PASS=changeme
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=shopping_cart

# Frontend API base (for local dev)
REACT_APP_API_BASE=http://localhost:8080
